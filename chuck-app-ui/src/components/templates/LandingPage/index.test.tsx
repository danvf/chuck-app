import { render } from "@testing-library/react";
import { LandingPage } from ".";

describe("Templates -> LandingPage", () => {
  it("should render the landing page with all its sections", () => {});
});
